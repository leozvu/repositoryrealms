export const metadata = {
  title: 'Realm Design System v2 · Preview',
  description: 'Permission-safe preview of the Realm enterprise operations design system.',
  robots: { index: false, follow: false },
};

export default function RealmV2Layout({ children }) {
  return children;
}
